schema_version: 1
name: loss-correction
description: Transition estimation followed by Forward correction smoke recipe.
steps:
  - {block: set_seed, params: {seed: 1}}
  - {block: prepare_paper_batch}
  - {block: estimate_transition, params: {save_as: estimated_transition}}
  - {block: forward_correction, params: {transition: estimated_transition}}
  - {block: mean_loss}
