schema_version: 1
name: volminnet
description: VolMinNet noisy NLL plus minimum-volume objective smoke recipe.
steps:
  - {block: set_seed, params: {seed: 1}}
  - {block: prepare_paper_batch}
  - {block: volminnet_objective}
